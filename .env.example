# Copy to .env for `netlify dev` (gitignored). In production these live in
# Netlify → Site configuration → Environment variables.

# Boats only. Free key from https://aisstream.io — one concurrent connection
# per key, so a local `netlify dev` and the deployed site will fight over it.
# Without it /api/vessels returns 501 and every other feed still works.
AISSTREAM_API_KEY=

# Optional, comma-separated extra origins allowed to call /api/* (see
# netlify/functions/_origin.js). Netlify already injects URL and
# DEPLOY_PRIME_URL, so leave this empty unless another site must call in.
# Unset locally means "no allowlist configured" — nothing is rejected.
ALLOWED_ORIGINS=
